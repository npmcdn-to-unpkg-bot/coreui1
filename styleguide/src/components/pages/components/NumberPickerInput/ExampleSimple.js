import React from 'react';
import NumberPickerInput from 'coreui/components/NumberPickerInput';

const NumberPickerInputExampleSimple = () => (
  <div>
    <NumberPickerInput defaultValue={1} />
  </div>
);

export default NumberPickerInputExampleSimple;
