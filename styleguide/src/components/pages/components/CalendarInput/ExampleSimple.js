import React from 'react';
import CalendarInput from 'coreui/components/CalendarInput';

const CalendarInputExampleSimple = () => (
  <div>
    <CalendarInput defaultValue={new Date()} />
  </div>
);

export default CalendarInputExampleSimple;
