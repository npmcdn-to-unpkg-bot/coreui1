import React from 'react';
import DateTimePickerInput from 'coreui/components/DateTimePickerInput';

const DateTimePickerInputExampleSimple = () => (
  <div>
    <DateTimePickerInput defaultValue={new Date()} />
  </div>
);

export default DateTimePickerInputExampleSimple;
